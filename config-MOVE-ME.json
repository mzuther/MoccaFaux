{
  "probing-interval": 60,

  "monitor": [
    {
      "name":    "important-processes",
      "enabled": true,
      "command": "pgrep -af '^dpkg\\b|^pacman\\b|^dar\\s|^/\\S*rsync\\b|^ssh\\s|^unison\\S*'",

      "tasks": {
        "sleep": true
      }
    },

    {
      "name":     "audio",
      "enabled":  true,
      "command":  "pactl list short sinks | grep -E '\\bRUNNING$'",

      "tasks": {
        "sleep": true
      }
    },

    {
      "name":     "video",
      "enabled":  true,
      "command":  "pgrep -l '^(celluloid|skypeforlinux|vlc)$'",

      "tasks": {
        "dpms": true,
        "sleep": true
      }
    },

    {
      "name":     "zfs-scrub",
      "enabled":  true,
      "command":  "zpool status | grep -E '(resilver|scrub) in progress'",

      "tasks": {
        "sleep": true
      }
    }
  ],

  "sleep-enable-cmd": {
    "command": "xautolock -time 15 -locker \"systemctl suspend\" -detectsleep",
    "fork":    true
  },

  "sleep-disable-cmd": {
    "command": "xautolock -exit",
    "fork":    false
  },

  "dpms-enable-cmd": {
    "command": "xset dpms 300 600 900",
    "fork":    false
  },

  "dpms-disable-cmd": {
    "command": "xset -dpms",
    "fork":    false
  }
}
